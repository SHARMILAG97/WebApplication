package com.revature.WaterPlant.Validator;

import com.revature.WaterPlant.dao.UserDao;
import com.revature.WaterPlant.dao.UserInterface;
import com.revature.WaterPlant.exception.ValidatorException;
import com.revature.WaterPlant.model.User;


public class UserValidator {

	public static void validateMobileNo(Long mno) throws ValidatorException {

		String num = String.valueOf(mno);

		if (num.length() != 10)

			throw new ValidatorException("Invalid Mobile Number");

	}
	
	

	public static void validateNameBeforeRegistration(User user) throws ValidatorException {
		
		//1. Check name is not null
		//User user = new User();
		String name = user.getName();
		if ( name == null || "".equals(name.trim())) 
			throw new ValidatorException("Invalid Name");
		}

}
